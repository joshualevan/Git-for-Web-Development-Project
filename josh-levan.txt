https://codepen.io/josh-levan/pen/poNQNBM?editors=1100

! INSERT QUESTIONS => ANSWER => PUSH and make PR

      1. What is Semantic HTML? 
    => HTML tags that more specific than "div." Used for SEO and reader programs.
      2. What is HTML used for? 
    => Hyper Text Markup Language -> The structure/ skeleton of a web page.
      3. What is an attribute and where do we put it?
    => Addition information inside of a tag eg. <img src="attribute"/>
      4. What is the h1 tag used for? How many times should I use it on a page?
    => It notes the most important title of  page and should only be used once.
      5. Name two tags that have required attributes
    => <img/ src="#"> <a href="#"></a>
      6. What do we put in the head of our HTML document?
    => Meta data, links, and the page title. 
      7. What is an id?
    => A unique attribute for a tag.
      8. What elements can I add an id to?
    => Any HTML attribute
      9. How many times can I use the same id on a page?
    => It's bad practice to use an id more than once. 
      10. What is a class?
    => a reusable attribute used as a selector 
      11. What elements can I add a class to?
    => Any HTML element 
      12. How many times can I use the same class on a page?
    => As many times as you want 
      13. How do I get my link to open in a new tab?
    => <a href="#" target="_blank"></a>
      14. What is the alt attribute in the image tag used for?
    => It's a descriptor for an img element 
      15. How do I reference an id?
    => #id-name
      16. What is the difference between a section and a div
    => A section is a semantic tag containing related elements. A div is a generic container
      17. What is CSS used for?
    => It's a styling sheet used linked to an HTML file 
      18. How to we select an element? Example - every h2 on the page
    => h2 {
        color: red;
    }
      19. What is the difference between a class and an id? - Give me an example of when I might use each one
    => An id is more specific while a class is more broad and able to be reused on elements with shared styles.
    <a href="#about">About</a>
    <section id= "about">
        <img src= "./image1" class= "gallery-photo"/>
        <img src= "./image2" class= "gallery-photo"/>
        <img src= "./image3" class= "gallery-photo"/>
    </section>
      20. How do we select classes in CSS?
    => .class-name {

    }
      21. How do we select a p element with a single class of “human””?
    => p.human {

      }
      22. What is a parent child selector? When would this be useful?
    =>  It is more specific. .gallery a {} is more specific than a {}. If you wanted all the <a> tags to share all the same styling, but wanted all the tags in the gallery to be, for example, opacity: 0.5 until hovered over— you could specify by adding the parent class. 
      23. How do you select all links within a div with the class of sidebar?
    => .sidebar a {

      }
      24. What is a pseudo selector?
    => changes style on event like hover, active, etc...
      25. What do we use the change the spacing between lines?
    => line-height
      26. What do we use to change the spacing between letters?
    => letter-spacing
      27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
    => text-transform: uppercase; lowercase; capitalize;
      28. How do I add a 1px border around my div that is dotted and black?
    => border: 1px dotted black;
      29. How do I select everything on the page?
    => * {

    } 
      30. How do I write a comment in CSS?
    => /* Comment */
      31. How do I find out what file I am in, when I am using the command line?
    => pwd 
      32. Using the command line - how do I see a list of files/folders in my current folder?
    => ls
      33. How do I remove a file via the command line? Why do I have to be careful with this?
    => rm file-name    *the is no way to undo deletion and no otion to cancel
      34. Why should I use version control?
    => it is easier than saving new file after new file when writing code or taking a new direction. Also, it is important when several coders are working on one project. 
      35. How often should I commit to github?
    => As often as you can without being redundant
      36. What is the command we would use to push our repo up to github?
    => $ git push -u origin repo-name then just $ git push 
      37. Walk me through Lambda's git flow.
    => https://www.notion.so/GitFlow-step-by-step-a764163309754e23b1b9c4077b1653cc

        - Fork- from source to your own repo
        - Copy clone url
        - Clone into Dir w/— $ git clone {pasted url}
        - $ cd into dir and start working w/ $ code .
        - $ git branch to check current branch
        - $ git checkout -b “{firstName-lastName}” to create and switch to new branch
        - OR - $ git checkout {firstName-lastName} to switch to your branch if already created
        - $ git add . Or git add {fileName} your changes (use $git status to see current state}
        - $ git commit -m “insert descriptive message”
        - $ git push -u origin {branch name} to initiate a stream (-u = “up stream”)
        - Repeat change file => add => commit => $ git push (stream already established)
        - PR (pull request) through gitHub on browser
            - Select pull request tab
            - Make new pull request
            - Select base (where to merge repo to) and compare (branch to merge from) 
  
  Stretch Questions
  
      1. What is the difference between an inline element and a block element?
    => an inline element like <span> does not break a line while a block element like <p> or <h1> will.
      2. What happens when an element is positioned absolutely? 
    => it breks from the flow and is positioned relative to it's nearest ancestor
      3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
    =>  specify a width in CSS
      4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    => Block- <h2>, <p>, <ul> Inline- <span>, <em> Inline-block- <img>
      5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
    => The box-model is like each element haning a series of shells. Starting with the content => padding => border => then ending with margin